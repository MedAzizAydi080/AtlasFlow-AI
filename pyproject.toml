[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--strict-markers"

[tool.ruff]
target-version = "py311"
line-length = 100
exclude = ["demo.excalidraw"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
